# ---- Google Sheets wiring (all optional; without them the app runs on the bundled snapshot) ----
# The spreadsheet that is the live database
GOOGLE_SHEET_ID=1qleRsOBr82SjoqVDLZTtsx2R47EBZqfRq0Yrv8rIyU0
# Service-account email (share the sheet with this address as Editor)
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# Service-account private key. Paste the full key; keep the literal \n escapes from the JSON.
GOOGLE_PRIVATE_KEY=
# Simple shared passphrase that staff type to unlock write actions (dispense/receive)
APP_WRITE_PIN=1234
# Optional: where low-stock email digests should be acknowledged (display only)
LOW_STOCK_CONTACT=
